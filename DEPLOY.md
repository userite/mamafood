# 🚀 Инструкции за разположение онлайн

## Push Notifications - Как работят?

Push notifications се изпращат автоматично:
- Когато порция изтича в следващите 15 минути
- На всички устройства свързани с същия `child_code`
- Работят дори когато приложението е затворено (ако е инсталирано като PWA)

## Как да разположиш онлайн

### Вариант 1: Node.js хостинг (Препоръчително)

#### Стъпки:

1. **Избери хостинг:**
   - **Heroku** (безплатно): https://www.heroku.com
   - **Railway** (безплатно): https://railway.app
   - **Render** (безплатно): https://render.com
   - **DigitalOcean** (платено): https://www.digitalocean.com

2. **Приготви файловете:**
   - Качи **ЦЯЛАТА папка** (не само index.html!)
   - Трябва: `server.js`, `package.json`, `index.html`, `app.js`, `styles.css`, и т.н.

3. **Конфигурирай:**
   
   **За Heroku:**
   - Създай `Procfile` (без разширение):
     ```
     web: node server.js
     ```
   - MySQL базата - използвай **JawsDB** или **ClearDB** добавка
   - Промени `dbConfig` в `server.js` да чете от `process.env`:
     ```javascript
     const dbConfig = {
         host: process.env.DB_HOST || '78.142.63.8',
         user: process.env.DB_USER || 'inex_mamafood_user',
         password: process.env.DB_PASSWORD || 'Parola_123!@#',
         database: process.env.DB_NAME || 'inex_deklarado',
         // ...
     };
     ```

4. **Deploy:**
   - Инсталирай Heroku CLI
   - `heroku create mamafood-app`
   - `git push heroku main`
   - Отиди на https://mamafood-app.herokuapp.com

### Вариант 2: Твой MySQL сървър (като теб)

Ако имаш сървър достъпен от интернет:

1. **Промени `server.js`:**
   - Промени `API_BASE` в `app.js` да не е `window.location.origin`
   - Използвай пълен URL: `const API_BASE = 'https://твоят-домейн.com';`

2. **Използвай сървър с Node.js:**
   - SSH към сървъра
   - Инсталирай Node.js: `curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash - && sudo apt-get install -y nodejs`
   - Качи файловете
   - Инсталирай: `npm install`
   - Стартирай: `node server.js` (или използвай PM2: `pm2 start server.js`)

3. **За стабилност използвай PM2:**
   ```bash
   npm install -g pm2
   pm2 start server.js --name mamafood
   pm2 save
   pm2 startup
   ```

## Тестване от 2 устройства

1. **Отвори приложението на Device 1:**
   - Въведи/генерирай child код (напр. `TEST01`)
   - Разреши notifications
   
2. **Отвори приложението на Device 2:**
   - Въведи **същия** child код (`TEST01`)
   - Разреши notifications

3. **Тествай:**
   - На Device 1, натисни бутона 🔔 за тест push
   - Или добави порция, която изтича след 10 минути
   - Device 2 трябва да получи известие!

## Важни бележки

⚠️ **HTTPS е задължителен** за push notifications!
- Без HTTPS push notifications НЕ работят
- Heroku, Railway, Render дават HTTPS автоматично

⚠️ **Database достъп:**
- MySQL сървърът трябва да е достъпен от интернет
- Провери firewall настройки на MySQL сървъра (порт 3306)
- Провери че потребителят може да се свърже от вън

## Локално тестване (без интернет)

За тестване локално на 2 устройства:

1. **На Windows (хост):**
   - Стартирай `node server.js`
   - Намери своя IP: `ipconfig` → IPv4 адрес (напр. `192.168.1.100`)

2. **На Device 1 и 2:**
   - Отвори `http://192.168.1.100:3000`
   - ⚠️ Push notifications НЕ работят на HTTP - само HTTPS!

3. **Алтернатива:**
   - Използвай `ngrok` за HTTPS tunnel:
     ```bash
     ngrok http 3000
     ```
   - Това ще даде URL като `https://xyz.ngrok.io`
   - Използвай този URL на двата устройства

## Проверка на push notifications

1. Отвори конзолата (F12)
2. Търси: `🔔 Push subscribed`
3. Натисни 🔔 бутона за тест
4. Трябва да видиш известие!

---

## Стъпки за деплой (кратко)

```bash
# 1. Подготви проект
npm install

# 2. Тествай локално
node server.js

# 3. За Heroku
heroku create mamafood-app
git push heroku main

# 4. Конфигурирай DB
heroku addons:create jawsdb:kitefin

# 5. Провери
heroku open
```

Готово! 🎉

