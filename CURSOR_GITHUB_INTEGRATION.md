# 🔗 GitHub Интеграция с Cursor IDE

## Какво означава това?

Когато Cursor показва че има GitHub интеграция за твоя repository, това означава че:

1. **Cursor може да вижда промените** в Git repository-то
2. **Можеш да правиш commit и push директно от Cursor** (без да използваш командния ред)
3. **Виждаш Git историята** и diff-овете в Cursor
4. **Можеш да синхронизираш** с GitHub директно от IDE-то

---

## 🎯 Какво можеш да правиш от Cursor

### 1. Виждане на промените

- Cursor показва кои файлове са променени (зелени/червени индикатори)
- Можеш да видиш diff-овете директно в редактора
- Виждаш Git историята

### 2. Commit и Push от Cursor

**От Source Control панела (Ctrl+Shift+G):**
- Виждаш всички променени файлове
- Можеш да добавиш файлове (Stage)
- Можеш да направиш commit с message
- Можеш да push-неш в GitHub

### 3. Синхронизация

- Pull промени от GitHub
- Push промени към GitHub
- Виждаш дали има конфликти

---

## 📝 Как да използваш GitHub интеграцията в Cursor

### Стъпка 1: Отвори Source Control

1. Натисни **Ctrl+Shift+G** (или кликни на иконата в ляво)
2. Ще видиш всички променени файлове

### Стъпка 2: Stage файловете

1. Кликни на **"+"** до файловете които искаш да добавиш
2. Или кликни на **"+"** до "Changes" за да добавиш всички

### Стъпка 3: Направи Commit

1. Напиши commit message в полето горе
2. Натисни **Ctrl+Enter** (или кликни на иконата за commit)
3. Избери "Commit" или "Commit & Push"

### Стъпка 4: Push в GitHub

1. Ако не си избрал "Commit & Push", натисни на **"..."** (три точки)
2. Избери **"Push"** или **"Push to..."**

---

## ✅ Предимства

### Предимства на Cursor интеграцията:

1. **По-лесно** - не трябва да използваш командния ред
2. **Визуално** - виждаш промените преди commit
3. **Бързо** - всичко е на едно място
4. **Безопасно** - можеш да видиш diff преди commit

### Кога да използваш commit-backend.bat:

- Когато искаш автоматизация
- Когато искаш да качиш само backend-only директорията
- Когато искаш batch операции

---

## 🔄 Синхронизация с Render.com

Ако backend-ът е свързан с Render.com чрез GitHub:

1. **Push от Cursor** → GitHub
2. **Render.com автоматично deploy-ва** новите промени
3. **Няма нужда от ръчно deploy** на Render.com

---

## 🛠️ Настройки

### Проверка на Git настройките в Cursor

1. Отвори Settings (Ctrl+,)
2. Търси "Git"
3. Провери настройките за:
   - Auto fetch
   - Auto push
   - Commit message template

### GitHub Authentication

Ако Cursor поиска authentication:
1. Ще те пренасочи към GitHub
2. Авторизирай достъпа
3. Cursor ще запомни credentials-ите

---

## 📋 Пример: Commit и Push от Cursor

### За backend промени:

1. Направи промени в `backend-only/server_pg.js`
2. Натисни **Ctrl+Shift+G**
3. Виждаш `server_pg.js` в Changes
4. Кликни **"+"** за да го stage-неш
5. Напиши commit message: `"Update backend: improve diagnostics"`
6. Натисни **Ctrl+Enter**
7. Избери **"Commit & Push"**
8. Готово! Промените са в GitHub и Render.com ще deploy-не автоматично

---

## ⚠️ Важно

### Какво НЕ трябва да commit-ваш:

- `.env` файлове (съдържат чувствителни данни)
- `node_modules/` (твърде големи)
- Локални конфигурационни файлове

### Проверка преди commit:

1. Виж diff-а (кликни на файла в Source Control)
2. Увери се че не commit-ваш `.env` или други чувствителни файлове
3. Провери commit message-а

---

## 🎉 Заключение

GitHub интеграцията в Cursor прави работата с Git много по-лесна и визуална. Можеш да използваш или Cursor, или `commit-backend.bat` - и двата метода работят!

**Препоръка**: Използвай Cursor за ежедневни промени, и `commit-backend.bat` за специфични операции.

