# ⚠️ ВАЖНО: Как да стартираш приложението правилно

## ❌ НЕ прави това:
- **НЕ** отваряй `index.html` директно с двоен клик
- **НЕ** използвай `file://` протокол

## ✅ Правилно стартиране:

### Вариант 1: Използвай batch файловете (препоръчително)

1. **Стартирай backend сървъра:**
   ```bash
   start-backend.bat
   ```
   Изчакай да видиш: `🚀 МАМАФООД Backend API running on port 3000`

2. **Стартирай frontend сървъра:**
   ```bash
   start-frontend.bat
   ```
   Това ще:
   - Стартира HTTP сървър на порт 8000
   - Отвори браузъра автоматично на `http://localhost:8000`

### Вариант 2: Ръчно стартиране

1. **Backend:**
   ```bash
   npm start
   ```

2. **Frontend (в нов терминал):**
   ```bash
   http-server -p 8000
   ```
   След това отвори в браузър: `http://localhost:8000`

## 🔍 Как да разпознаеш проблемите:

### Ако виждаш тези грешки:
- `origin 'null'` 
- `file://` в URL-а
- `CORS policy` грешки
- `Service Worker registration failed`

**Причина:** Отваряш файла директно вместо чрез HTTP сървър

**Решение:** Използвай `start-frontend.bat` или `http-server`

## 📝 Забележки:

- Backend сървърът трябва да работи на `http://localhost:3000`
- Frontend трябва да се отваря на `http://localhost:8000` (не file://)
- Ако имаш проблеми, винаги стартирай първо backend, после frontend

