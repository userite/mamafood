# 🚀 Бързо ръководство за Online Deployment

## ⚡ Стъпки за качване на production

### 1️⃣ Промени Render.com URL в app_v2.js

**Отвори `app_v2.js` и намери ред 544 и 548:**

```javascript
// Ред 544 - за pci.inex-project.net
return 'https://mamafood-backend.onrender.com';  // ← ПРОМЕНИ!

// Ред 548 - default
return 'https://mamafood-backend.onrender.com';  // ← ПРОМЕНИ!
```

**Замени с твоя действителен Render.com backend URL!**

Например:
```javascript
return 'https://mamafood-api-xyz123.onrender.com';
```

---

### 2️⃣ Конфигурирай deploy-frontend.bat

**Отвори `deploy-frontend.bat` и промени:**

```batch
set "FTP_USER=твоят_потребител"        ← ПРОМЕНИ!
set "FTP_PASS=твоята_парола"            ← ПРОМЕНИ!
set "FTP_DIR=/public_html/mamafood"     ← ПРОМЕНИ според твоята структура
```

---

### 3️⃣ Качи файловете

```batch
deploy-frontend.bat
```

---

### 4️⃣ Тествай

1. **Backend Health Check:**
   ```
   https://твоят-backend-url.onrender.com/api/health
   ```

2. **Frontend:**
   ```
   https://pci.inex-project.net/mamafood/
   ```

3. **Провери конзолата (F12)** за грешки

---

## ✅ Checklist

- [ ] Backend URL променен в `app_v2.js` (2 места)
- [ ] FTP credentials конфигурирани в `deploy-frontend.bat`
- [ ] Файловете качени успешно
- [ ] Backend health check работи
- [ ] Frontend се зарежда без грешки
- [ ] Записите се показват правилно

---

## 🛠️ Ако има проблеми

**CORS грешка?**
- Провери дали `pci.inex-project.net` е в CORS списъка (вече е!)

**"Failed to fetch"?**
- Провери дали backend URL в `app_v2.js` е правилен
- Провери дали backend работи на Render.com

**Записите не се показват?**
- Провери конзолата (F12) за грешки
- Провери дали API заявката връща данни

---

## 📚 Пълно ръководство

За подробни инструкции виж: `DEPLOY_ONLINE.md`

