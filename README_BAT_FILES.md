# 📝 Описание на BAT файловете

## 🚀 Локално тестване

### `start-backend.bat`
Стартира backend сървъра локално.

**Какво прави:**
- Променя директорията към `backend-only/`
- Проверява и инсталира зависимости (`npm install`)
- Стартира сървъра на `http://localhost:3000`

**Използване:**
Двойно кликване на файла или:
```batch
start-backend.bat
```

**За локално тестване** създай `backend-only/.env`:
```
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mamafood
PORT=3000
```

---

### `start-frontend.bat`
Стартира frontend сървъра локално.

**Какво прави:**
- Проверява и инсталира `http-server` ако не е наличен
- Стартира frontend на `http://localhost:8000`
- Автоматично отваря браузъра

**Използване:**
Двойно кликване на файла или:
```batch
start-frontend.bat
```

**Важно:** Backend трябва да работи на `http://localhost:3000`!

---

## 🌐 Production Deployment

### `deploy-frontend.bat`
Качва frontend файловете на pci.inex-project.net чрез curl/FTP.

**Какво прави:**
- Качва следните файлове:
  - `index.html`
  - `app_v2.js`
  - `i18n.js`
  - `styles.css`
  - `manifest.json`
  - `service-worker.js`

**Преди използване:**
1. Отвори `deploy-frontend.bat` с текстов редактор
2. Промени FTP credentials:
   ```batch
   set "FTP_USER=твоят_потребител"
   set "FTP_PASS=твоята_парола"
   set "FTP_DIR=/public_html/mamafood"
   ```
3. Запази файла

**Използване:**
Двойно кликване на файла или:
```batch
deploy-frontend.bat
```

**Изисквания:**
- `curl` трябва да е инсталиран (обикновено е включен в Windows 10+)
- Провери: `curl --version` в Command Prompt

---

## 📋 Ред на изпълнение

### За локално тестване:
1. Пусни `start-backend.bat` (в отделен прозорец)
2. Пусни `start-frontend.bat` (в друг прозорец)
3. Отвори `http://localhost:8000` в браузъра

### За production deployment:
1. Качи backend на Render.com (чрез GitHub)
2. Промени `app_v2.js` с правилния Render.com URL
3. Конфигурирай `deploy-frontend.bat` с FTP credentials
4. Пусни `deploy-frontend.bat`
5. Провери приложението на pci.inex-project.net

---

## ⚠️ Важни бележки

1. **Backend URL**: Преди качване на frontend, уверете се че `app_v2.js` е променен с правилния Render.com URL
2. **FTP Credentials**: Никога не commit-вай `deploy-frontend.bat` с реални пароли в GitHub!
3. **CORS**: Уверете се че `pci.inex-project.net` е в CORS списъка в `server_pg.js`

---

## 🛠️ Отстраняване на проблеми

### Backend не стартира
- Провери дали `backend-only/server_pg.js` съществува
- Провери дали `backend-only/package.json` съществува
- Провери дали Node.js е инсталиран: `node --version`

### Frontend не стартира
- Провери дали `index.html` съществува
- Провери дали `http-server` е инсталиран: `http-server --version`
- Опитай ръчно: `npm install -g http-server`

### Deploy не работи
- Провери дали `curl` е инсталиран: `curl --version`
- Провери FTP credentials в `deploy-frontend.bat`
- Провери дали FTP директорията съществува на сървъра

---

## 📚 Допълнителна информация

За пълни инструкции виж:
- `DEPLOY_BG.md` - Кратко ръководство на български
- `DEPLOY_INSTRUCTIONS.md` - Подробни инструкции на английски

