# 🎯 ЗАПОЧНИ ТУК - Стъпка по стъпка

## ✅ Какво вече е готово:
- ✅ Файловете са приготвени в `backend-only/`
- ✅ Git е инициализиран
- ✅ Всички файлове са commit-нати

---

## 📋 СЛЕДВАЩИ СТЪПКИ (в този ред):

### ⭐ СТЪПКА 1: Създай GitHub Repository

#### 1.1. Отвори браузър:
Отиди на: **https://github.com**

#### 1.2. Логни се (или създай акаунт)
- Ако нямаш: "Sign up" (безплатно)
- Ако имаш: "Sign in"

#### 1.3. Създай нов repository:
1. Натисни **"+"** (горе в дясно)
2. Избери **"New repository"**
3. Попълни:
   - **Repository name:** `mamafood-backend`
   - **Description:** (остави празно или напиши нещо)
   - **Public** или **Private** (избери сам)
   - ❌ **НЕ** тиквай нищо друго (няма нужда от README, .gitignore, license)
4. Натисни **"Create repository"**

#### 1.4. GitHub ще покаже страница с инструкции

Избери: **"...or push an existing repository from the command line"**

Ще видиш команди като:
```
git remote add origin https://github.com/ТВОЕТО-ИМЕ/mamafood-backend.git
git branch -M main
git push -u origin main
```

**Но ПЪРВО:**
1. Копирай URL-а (примерно: `https://github.com/username/mamafood-backend.git`)
2. Кажи ми го тук и ще ти дам точните команди!

**ИЛИ:**

Качи файловете през браузъра:
1. В GitHub страницата на новия repository
2. Натисни **"uploading an existing file"**
3. Drag & drop или Browse:
   - `server.js`
   - `package.json`
   - `Procfile`
   - `README.md` (ако искаш)
4. Натисни **"Commit changes"**

---

### ⭐ СТЪПКА 2: Качи файловете на GitHub

**Вариант A: От командния ред (PowerShell)**

В PowerShell (все още в `backend-only/`), изпълни:

```powershell
# Добави remote (ЗАМЕНИ с твоя URL):
git remote add origin https://github.com/ТВОЕТО-ИМЕ/mamafood-backend.git

# Промени branch на main (ако е нужно):
git branch -M main

# Push файловете:
git push -u origin main
```

**Ако те пита за username/password:**
- Username: твоя GitHub username
- Password: **НЕ** паролата ти, а **Personal Access Token**

**Как да създадеш Token:**
1. GitHub → Settings (профил иконка → Settings)
2. Отляво: **"Developer settings"**
3. **"Personal access tokens"** → **"Tokens (classic)"**
4. **"Generate new token"** → **"Generate new token (classic)"**
5. Дай име: "mamafood-deploy"
6. Избери: **"repo"** (чекбокс)
7. Скрол надолу → **"Generate token"**
8. **КОПИРАЙ ТОКЕНА** (ще го видиш само веднъж!)
9. Използвай го като парола при push

**Вариант B: GitHub Desktop (ПО-ЛЕСНО!)**

1. Сваляш от: https://desktop.github.com
2. Инсталирай
3. Логни се с GitHub акаунта
4. File → Clone repository → избери твоя `mamafood-backend`
5. Копирай файловете от `backend-only/` в клонираната папка
6. GitHub Desktop ще види промените
7. Commit message: "Initial commit"
8. Натисни **"Commit to main"**
9. Натисни **"Push origin"**

**Така е много по-лесно!** 🎯

---

### ⭐ СТЪПКА 3: Създай Railway акаунт

1. Отиди на: **https://railway.app**
2. Натисни **"Start a New Project"**
3. Избери **"Continue with GitHub"**
4. Разреши достъп (Railway ще вижда твоите repositories)

---

### ⭐ СТЪПКА 4: Деплой на Railway

1. **"New Project"** → **"Deploy from GitHub repo"**
2. Избери: `mamafood-backend`
3. Railway автоматично ще започне деплой
4. Изчакай 1-2 минути

---

### ⭐ СТЪПКА 5: Database настройки

1. Кликни на приложението
2. **"Variables"** tab
3. Добави:
   - `DB_HOST` = `78.142.63.8`
   - `DB_USER` = `inex_mamafood_user`
   - `DB_PASSWORD` = `Parola_123!@#`
   - `DB_NAME` = `inex_deklarado`
   - `NODE_ENV` = `production`

---

### ⭐ СТЪПКА 6: Запиши URL и тествай

1. **"Settings"** tab
2. Скрол до **"Domains"**
3. Копирай URL (примерно: `https://mamafood-production.up.railway.app`)
4. Тествай: `https://твоят-url.up.railway.app/api/health`
5. Трябва да видиш: `{"status":"ok","timestamp":"..."}`

✅ **Готово!**

---

## 🆘 Къде закъса?

Кажи ми на коя стъпка си и какъв точно проблем имаш, и ще помогна конкретно!

---

## 💡 МОЯТА ПРЕПОРЪКА:

Използвай **GitHub Desktop** за качване на файловете - много по-лесно от командния ред!




