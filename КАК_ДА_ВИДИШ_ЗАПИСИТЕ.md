# Как да видиш записите за код "KAI"

## Проблем: Има записи в базата, но не се показват в приложението

### Стъпка 1: Проверка на backend сървъра

Увери се, че backend сървърът работи:
```bash
start-backend.bat
```

Трябва да виждаш:
- ✅ `Database connection successful`
- ✅ `Table "records" is ready`
- ✅ `🚀 МАМАФООД Backend API running on port 3000`

### Стъпка 2: Тест на API endpoint-ите

Отвори в браузър или използвай `test-api.bat`:

**Health check:**
```
http://localhost:3000/api/health
```
Трябва да върне: `{"status":"ok","timestamp":"..."}`

**Проверка на записи за "KAI":**
```
http://localhost:3000/api/records/KAI
```
Трябва да върне JSON масив с 3 записа.

**Проверка на дете "KAI":**
```
http://localhost:3000/api/children/KAI
```

### Стъпка 3: Проверка на childCode в localStorage

1. Отвори приложението в браузър
2. Натисни **F12** (Developer Tools)
3. Отиди на вкладка **Console**
4. Въведи:
```javascript
localStorage.getItem('mamafood_child_code')
```

**Ако не е "KAI":**
```javascript
localStorage.setItem('mamafood_child_code', 'KAI');
location.reload();
```

### Стъпка 4: Проверка на API заявките

1. Отвори приложението в браузър
2. Натисни **F12** (Developer Tools)
3. Отиди на вкладка **Network**
4. Презареди страницата (F5)
5. Търси заявки към `/api/records/KAI`
6. Кликни на заявката и провери:
   - **Status**: Трябва да е `200 OK`
   - **Response**: Трябва да виждаш JSON с записите

### Стъпка 5: Проверка на конзолата за грешки

В браузър конзолата (F12 → Console), провери за:
- ❌ CORS грешки
- ❌ Network грешки
- ❌ JavaScript грешки

### Стъпка 6: Ръчно задаване на код "KAI"

Ако нищо не работи, задай кода ръчно:

**Вариант 1: Чрез модал прозорец**
1. Кликни на бутона 👶 (код на детето) в header-а
2. Въведи: **KAI**
3. Кликни "Запази"

**Вариант 2: Чрез конзола**
```javascript
// Задай кода
localStorage.setItem('mamafood_child_code', 'KAI');

// Презареди страницата
location.reload();
```

**Вариант 3: Чрез функцията**
```javascript
// В конзолата
setChildCode();
// В появилия се prompt въведи: KAI
```

## Често срещани проблеми

### Проблем 1: Backend сървърът не работи
**Решение:** Стартирай `start-backend.bat`

### Проблем 2: CORS грешка
**Решение:** Провери дали backend сървърът работи и дали CORS е конфигуриран правилно

### Проблем 3: childCode не е "KAI"
**Решение:** Задай го ръчно (виж Стъпка 6)

### Проблем 4: API връща празен масив []
**Решение:** 
- Провери дали записите в базата са с точно код "KAI"
- Използвай `test-database.bat` за проверка

### Проблем 5: Записите се зареждат, но не се показват
**Решение:** 
- Провери конзолата за JavaScript грешки
- Провери дали `renderRecords()` функцията се извиква

## Полезни команди за дебъгване

**В браузър конзолата:**
```javascript
// Проверка на childCode
localStorage.getItem('mamafood_child_code')

// Проверка на записите в localStorage
JSON.parse(localStorage.getItem('mamafood_records'))

// Ръчно зареждане на записи
loadRecords()

// Проверка на API_BASE
API_BASE

// Ръчно тест на API заявка
fetch('http://localhost:3000/api/records/KAI').then(r => r.json()).then(console.log)
```

## След като коригираш

След като зададеш код "KAI" и backend сървърът работи, записите трябва да се появят автоматично при зареждане на страницата.

