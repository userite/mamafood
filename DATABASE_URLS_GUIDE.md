# 🔗 Ръководство за DATABASE_URL

## 📝 Как да конфигурираш DATABASE_URL

### За локално тестване (на твоя компютър)

Създай `.env` файл в `backend-only/` директорията:

```env
DATABASE_URL=postgresql://user:password@host:5432/mamafood
PORT=3000
```

**За локална база:**
```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mamafood
PORT=3000
```

**За Render.com база (External URL):**
```env
DATABASE_URL=postgresql://user:password@dpg-xxxxx-a.oregon-postgres.render.com:5432/mamafood_xxxx
PORT=3000
```

---

### За production на Render.com

#### Автоматично (препоръчително)

Render.com автоматично добавя `DATABASE_URL` като environment variable когато:
- Backend и PostgreSQL са в същия проект
- Или когато свържеш PostgreSQL към Web Service

**Не е нужно да създаваш .env файл!** Render.com автоматично добавя правилния URL.

#### Ръчно конфигуриране

В Render.com Dashboard → Web Service → Environment:

```
DATABASE_URL=postgresql://user:pass@host:5432/database
```

**Забележка**: Render.com може да предостави Internal URL (с `.internal` в hostname) когато services са в същия проект. Кодът автоматично разпознава Internal URL и изключва SSL когато е необходимо.

---

## 🔧 Как да зададеш DATABASE_URL

### Стъпка 1: Вземи connection string от Render.com

1. Отиди на Render.com Dashboard
2. Избери PostgreSQL базата
3. В секцията "Connections" ще видиш connection string

### Стъпка 2: За локално тестване

Създай `backend-only/.env`:

```env
DATABASE_URL=postgresql://user:password@host:5432/database
PORT=3000
```

**Или използвай помощния скрипт:**
```batch
cd backend-only
create-env.bat
```

### Стъпка 3: За production на Render.com

**Не е нужно да правиш нищо!** Render.com автоматично добавя правилния URL.

---

## ✅ Проверка

След като конфигурираш, стартирай backend:

```batch
start-backend.bat
```

Трябва да видиш:
```
📊 Database Configuration:
   ✅ Използвам: postgresql://...
   🔒 SSL: Да/Не
✅ Database connection test successful: ...
```

---

## ❓ Често задавани въпроси

### Q: Как да знам кой URL се използва?

**A**: Провери конзолата на backend-а при стартиране - ще покаже кой URL се използва и дали SSL е включено.

### Q: Трябва ли SSL за локална база?

**A**: Не, SSL не е нужно за локална база (localhost). Кодът автоматично изключва SSL за localhost.

### Q: Какво да правя ако имам проблеми с връзката?

**A**: 
1. Провери дали `DATABASE_URL` е правилен в `.env` файла
2. Провери дали PostgreSQL е стартиран
3. Провери username и password в connection string-а

---

## 🎉 Готово!

След като конфигурираш правилния `DATABASE_URL`, backend-ът трябва да се свърже успешно с базата данни!

