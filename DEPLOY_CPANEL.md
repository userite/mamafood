# 🚀 Инструкция за разположение чрез cPanel и FTP

## Стъпки за качване и стартиране

### 1. Проверка на cPanel за Node.js поддръжка

1. **Влез в cPanel**
2. **Провери за "Node.js" приложение:**
   - Потърси в менюто "Node.js" или "Node.js Selector"
   - Ако го имаш → Продължи с Вариант A (Node.js App)
   - Ако НЕ го имаш → Използвай Вариант B (PHP Wrapper)

### Вариант A: С Node.js приложение в cPanel

#### Стъп 1: Създаване на Node.js приложение

1. **Отвори "Node.js Selector" в cPanel**
2. **Създай ново приложение:**
   - **Node.js версия:** Избери най-новата (18.x или 20.x)
   - **Application mode:** Production
   - **Application root:** `/home/tвоят_потребител/mamafood` (или друга директория)
   - **Application URL:** Избери поддомейн или подпапка (напр. `mamafood` или `app`)
   - **Application startup file:** `server.js`

3. **Натисни "Create"**

#### Стъп 2: Качване на файловете

**Използвай FTP клиент (FileZilla, WinSCP, или вградения в cPanel):**

1. **Отвори FileZilla или друг FTP клиент**
2. **Свържи се:**
   - **Host:** `pci.inex-project.net` (или FTP хост от cPanel)
   - **Username:** Твоят cPanel потребител
   - **Password:** Твоята парола
   - **Port:** 21 (FTP) или 22 (SFTP)
3. **Навигирай до директорията:**
   - Обикновено е: `/public_html/mamafood` или `/home/твоят_потребител/mamafood`
   - Или пътя, който указа в Node.js приложението

4. **Качи всички файлове:**
   - ✅ `server.js`
   - ✅ `package.json`
   - ✅ `index.html`
   - ✅ `app.js`
   - ✅ `styles.css`
   - ✅ `i18n.js`
   - ✅ `manifest.json`
   - ✅ `service-worker.js`
   - ✅ `Procfile` (опционално)
   - ✅ `.htaccess` (ако трябва)

**⚠️ НЕ качвай `node_modules/`** - зависимостите ще се инсталират автоматично от cPanel

#### Стъп 3: Конфигуриране

**Редактирай `server.js` в cPanel File Manager:**

1. Отвори File Manager в cPanel
2. Намери `server.js`
3. Редактирай `const PORT = process.env.PORT || 3000;`
   - cPanel автоматично задава PORT променливата
4. Провери дали database конфигурацията е правилна

**Редактирай `app.js`:**

Намери реда с `API_BASE` и промени на:
```javascript
const API_BASE = window.location.origin; // Това е правилно - автоматично ще използва правилния домейн
```

#### Стъп 4: Инсталация на зависимости

**В cPanel Node.js приложението:**

1. Отвори "Node.js Selector"
2. Намери твоето приложение
3. Кликни на "Run NPM Install" или "npm install"
4. Изчакай инсталацията да приключи

#### Стъп 5: Стартиране

1. В "Node.js Selector"
2. Намери приложението и натисни **"Restart"** или **"Start"**

Приложението вече работи!

---

### Вариант B: Без Node.js в cPanel (PHP Wrapper)

Ако нямаш Node.js в cPanel, можеш да използваш PHP wrapper или да запиташ хоста да инсталира Node.js.

#### Алтернатива 1: Запитай хоста за Node.js

Свържи се с поддръжката и запитай:
- "Имате ли Node.js поддръжка?"
- "Може ли да инсталирате Node.js на моя акаунт?"

#### Алтернатива 2: Използвай друг хостинг

За Node.js приложения, препоръчвам:
- **Heroku** (безплатно)
- **Railway** (безплатно)
- **Render** (безплатно)
- **DigitalOcean** (от $5/месец)

---

## Детайлна инструкция за FTP качване

### Стъпки:

1. **Изтегли FileZilla (безплатно):**
   - https://filezilla-project.org/

2. **Намери FTP данните в cPanel:**
   - Отвори "FTP Accounts"
   - Виж "FTP Username" и "FTP Server"
   - Или използвай основния cPanel потребител

3. **Свържи се в FileZilla:**
   ```
   Host: pci.inex-project.net
   Username: твоят_ftp_потребител
   Password: твоята_парола
   Port: 21
   ```

4. **Намери правилната директория:**
   - Обикновено: `/public_html/` или `/home/твоят_потребител/public_html/`
   - Ако имаш поддомейн: `/public_html/mamafood/`
   - Или в документацията на cPanel Node.js приложението

5. **Качи файловете:**
   - Избери всички файлове от проекта (без `node_modules/`)
   - Драг & drop в дясната панел
   - Изчакай качването

---

## Конфигурация специфична за cPanel

### .htaccess файл (ако е необходимо)

Ако имаш проблеми с рутинг, създай `.htaccess` файл:

```apache
RewriteEngine On
RewriteBase /

# Proxy към Node.js (ако Node.js приложението работи на друг порт)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^api/(.*)$ http://localhost:PORT/api/$1 [P,L]

# Serve static files
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Fallback към index.html
RewriteRule ^ index.html [L]
```

**Но обикновено cPanel Node.js приложенията се грижат за това автоматично.**

### Environment Variables в cPanel

Ако трябва да зададеш environment variables:

1. В Node.js приложението в cPanel
2. Потърси "Environment Variables" или "ENV Variables"
3. Добави:
   ```
   DB_HOST=78.142.63.8
   DB_USER=inex_mamafood_user
   DB_PASSWORD=Parola_123!@#
   DB_NAME=inex_deklarado
   NODE_ENV=production
   ```

---

## Проверка и тестване

1. **Отвори приложението:**
   - URL от cPanel Node.js приложението (напр. `https://pci.inex-project.net/mamafood`)
   - Или вярния URL според конфигурацията

2. **Провери:**
   - Отваря ли се страницата?
   - Работят ли бутоните?
   - Можеш ли да добавиш запис?

3. **Ако има грешки:**
   - Провери логовете в cPanel Node.js приложението
   - Или в "Error Log" на cPanel

---

## Полезни cPanel команди/раздели

- **File Manager** - за качване/редактиране на файлове
- **Node.js Selector** - за управление на Node.js приложения
- **Error Log** - за грешки
- **Terminal** (ако го имаш) - за команди
- **FTP Accounts** - за FTP настройки

---

## Често срещани проблеми

### Проблем 1: "Cannot find module"

**Решение:**
- Убеди се че `node_modules/` е инсталиран
- В cPanel Node.js приложението → "Run NPM Install"

### Проблем 2: "Port already in use"

**Решение:**
- cPanel управлява портовете автоматично
- Рестартирай приложението в Node.js Selector

### Проблем 3: "Database connection failed"

**Решение:**
- Провери MySQL достъп от сървъра към 78.142.63.8
- Може да трябва да разрешиш IP адреса в MySQL firewall

### Проблем 4: "404 Not Found" за API

**Решение:**
- Провери URL пътя в приложението
- Убеди се че `server.js` слуша правилно

---

## Структура на файлове

```
/home/твоят_потребител/
├── mamafood/                 (или друга директория)
│   ├── server.js            ✅ Качи
│   ├── package.json         ✅ Качи
│   ├── index.html           ✅ Качи
│   ├── app.js               ✅ Качи
│   ├── styles.css           ✅ Качи
│   ├── i18n.js              ✅ Качи
│   ├── manifest.json        ✅ Качи
│   ├── service-worker.js    ✅ Качи
│   ├── Procfile             ✅ Качи (опционално)
│   └── node_modules/        ❌ НЕ качвай (ще се инсталира автоматично)
```

---

## След качването

1. ✅ Провери че всички файлове са качени
2. ✅ Инсталирай зависимости през cPanel Node.js приложението
3. ✅ Рестартирай приложението
4. ✅ Тествай на URL-а от cPanel

---

**Готово! 🎉**

Ако имаш проблеми, провери Error Log в cPanel или се свържи с поддръжката на хостинга.




