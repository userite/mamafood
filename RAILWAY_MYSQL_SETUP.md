# 🚂 Railway - Backend + MySQL (Най-лесно решение!)

Railway е **най-лесното решение** - всичко на едно място! 🎯

---

## ✅ ПРЕДИМСТВА:

- ✅ **Безплатни $5 кредити/месец** (достатъчно за малък проект)
- ✅ **Backend + MySQL на едно място**
- ✅ **Автоматично деплой от GitHub**
- ✅ **Лесно управление**
- ✅ **Бързо стартиране**

---

## 🚀 СТЪПКА 1: Създай Railway акаунт

1. **Отиди на:** https://railway.app
2. **Натисни:** "Start a New Project"
3. **Логни се с GitHub** (най-лесно)
   - Или с Email + парола

**Готово!** Акаунтът се създава автоматично. ✅

---

## 🗄️ СТЪПКА 2: Създай MySQL база данни

1. В Railway dashboard → **"New"** (ляво горе)
2. **Избери:** "Database" → **"Add MySQL"**
3. Railway автоматично ще създаде MySQL инстанция
4. **Изчакай** 1-2 минути докато се създаде

**Готово!** MySQL базата е създадена! ✅

---

## 📋 СТЪПКА 3: Вземи Connection Details

1. **Кликни на MySQL service-а** (от списъка)
2. **Tab "Variables"** (или "Connect")
3. **Ще видиш:**
   ```
   MYSQLHOST=containers-us-west-xxx.railway.app
   MYSQLUSER=root
   MYSQLPASSWORD=xxxxx
   MYSQLDATABASE=railway
   MYSQLPORT=3306
   ```
4. **Копирай ги!** (ще ги трябват после)

---

## 💻 СТЪПКА 4: Създай Backend Service

1. **Railway dashboard → "New"**
2. **Избери:** "GitHub Repo" (или "Deploy from GitHub repo")
3. **Избери repository-то** (ако вече е в GitHub)
   - Или **"Configure GitHub App"** ако не е свързано
4. **Избери repository-то:** `Gabi_and_Kai` (или както се казва)

---

## ⚙️ СТЪПКА 5: Настрой Backend

1. **Кликни на backend service-а**
2. **Settings tab:**
   - **Root Directory:** `backend-only`
   - **Start Command:** `node server.js`
   - **Build Command:** (празно или `npm install`)

3. **Variables tab:**
   - Railway автоматично ще добави MySQL променливите
   - **Добави още:**
     ```
     PORT=3000
     NODE_ENV=production
     DB_SSL=true
     ```
   
   **ВАЖНО:** Railway автоматично добавя MySQL променливи като:
   - `MYSQLHOST` → трябва да го мапнеш към `DB_HOST`
   - `MYSQLUSER` → мапни към `DB_USER`
   - `MYSQLPASSWORD` → мапни към `DB_PASSWORD`
   - `MYSQLDATABASE` → мапни към `DB_NAME`

---

## 🔗 СТЪПКА 6: Свържи Backend с MySQL

Railway има **автоматично linking**, но ако не работи:

1. **Backend service → Settings**
2. **"Generate MySQL Connection URL"** (ако има такъв бутон)
3. Или **ръчно добави variables:**
   ```
   DB_HOST=${{MySQL.MYSQLHOST}}
   DB_USER=${{MySQL.MYSQLUSER}}
   DB_PASSWORD=${{MySQL.MYSQLPASSWORD}}
   DB_NAME=${{MySQL.MYSQLDATABASE}}
   DB_SSL=true
   PORT=3000
   ```

**Синтаксисът `${{Service.Variable}}` е Railway синтаксис за референция!**

---

## 📊 СТЪПКА 7: Създай таблиците

### Вариант A: От Railway Console

1. **MySQL service → "Data" tab**
2. **"Connect"** → ще отвори MySQL конзола
3. **Копирай и изпълни** SQL заявките от `database_setup.sql`

### Вариант B: От локалния компютър

1. **Инсталирай MySQL клиент:**
   ```powershell
   # Ако имаш MySQL Workbench или друг клиент
   ```

2. **Свържи се с Railway MySQL:**
   - Host: `containers-us-west-xxx.railway.app` (от MYSQLHOST)
   - User: `root` (от MYSQLUSER)
   - Password: (от MYSQLPASSWORD)
   - Port: `3306` (от MYSQLPORT)
   - Database: `railway` (от MYSQLDATABASE)

3. **Изпълни `database_setup.sql`**

---

## 🚀 СТЪПКА 8: Deploy!

1. **Railway автоматично ще деплойне** след push в GitHub
2. Или **"Deploy"** бутон в Railway dashboard
3. **Изчакай** докато се build-не
4. **Ще получиш URL:** `https://xxx.up.railway.app`

---

## ✅ СТЪПКА 9: Тествай!

1. **Отиди на:** `https://xxx.up.railway.app/api/health`
2. **Трябва да видиш:** `{"status":"ok"}`
3. **Ако работи:** Готово! 🎉

---

## 🐛 Често срещани проблеми

### "Cannot connect to MySQL"

**Решение:**
- Провери дали `DB_SSL=true` е зададено
- Провери дали MySQL променливите са правилно мапнати
- Провери logs в Railway (Backend service → "Deployments" → "View Logs")

### "Port already in use"

**Решение:**
- Railway автоматично задава PORT, не го задавай ръчно
- Премахни `PORT=3000` от variables

### "Database not found"

**Решение:**
- Провери дали таблиците са създадени
- Изпълни `database_setup.sql` отново

---

## 💰 Цени

- **Free Plan:** $5 кредити/месец (безплатно)
- **Developer Plan:** $20/месец (ако преминеш лимита)

**За малък проект:** Free плана е достатъчен! ✅

---

## 🎯 Следващи стъпки

1. **Frontend:** Можеш да качиш frontend-а на:
   - Railway (друг service)
   - Render.com (статичен сайт)
   - GitHub Pages
   - Netlify

2. **Domain:** Railway позволява custom domain (безплатно)

---

## ✅ Готово!

Сега имаш backend + MySQL работещи на Railway! 🚀

**URL-а на backend-а:** `https://xxx.up.railway.app`

**Добави го в `app_v2.js` като `API_BASE`!**

